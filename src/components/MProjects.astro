---
import MSectionHead from "./MSectionHead.astro";
import MSection from "./MSection.astro";
import MProjectItem from "./MProjectItem.astro";
import { PROJECTS_DATA } from "../@constants/projects";
---

<MSection>
  <MSectionHead>
    <span slot="title">Mis trabajos 👩‍💻</span>
    <span slot="text">
      He adquirido estas habilidades gracias a mi pasión por aprender.
    </span>
  </MSectionHead>
  <div class="projects__container">
    {
      PROJECTS_DATA.map((project) => (
        <MProjectItem
          slug={project.slug}
          title={project.title}
          image={project.image}
        />
      ))
    }
  </div>
</MSection>

<style>
  @reference "tailwindcss";

  .projects__container {
    @apply grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 p-14;
  }
</style>
